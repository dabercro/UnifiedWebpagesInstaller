# Unified Webpages

This is set up to install any webpages for Unified on anyone's AFS space with basically zero prerequisites.

To Do: <br>
Make installation instructions <br>
Add a script to update an existing webpage setup <br>
Clean up showlog to be fancy instead of just spitting out a long string of HTML tags
